on:
  push:
    branches:
      - main
name: 🚀 Deploy Production
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Checkout
        uses: actions/checkout@v1
      - name: Deploy Production server
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          ARGS: "-rlgoDzvc -i --delete"
          SOURCE: "./"
          REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          TARGET: "./menuOnline-frontend/"
          SCRIPT_AFTER: |         
            cd menuOnline-frontend
            echo 'APPLICATION_TYPE=prod;NEXT_PUBLIC_BACKEND_ENDPOINT=http://191.101.234.188:8080/api/graphql;NEXT_PUBLIC_MINIO_ENDPOINT=191.101.234.188;NEXT_PUBLIC_MINIO_PORT=9000;NEXT_PUBLIC_MINIO_USESSL=0;NEXT_PUBLIC_MINIO_ACCESSKEY=minio;NEXT_PUBLIC_MINIO_SECRETKEY=minio123;NEXT_PUBLIC_MINIO_BUCKET=menu;' > .env;